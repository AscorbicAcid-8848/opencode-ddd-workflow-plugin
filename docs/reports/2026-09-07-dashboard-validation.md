# DDD 控制面板验证记录

日期：2026-09-07。对照基线：`c14423b`。本次提交只包含控制面板、状态保存中的展示投影扩展、依赖与测试，不包含工作区原有的运行时修改。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| TypeScript 构建 | 通过 |
| 本次控制面板回归 | 10 / 10 通过 |
| 原生 OpenTUI 渲染与交互 | 通过，包含命令注册、页面渲染、切换里程碑、返回与清理 |
| 真实 `hmdp01` 历史读取 | 4 个归档流程正确展示，各 6 个里程碑已批准；未修改历史状态 |
| 当前完整工作区测试 | 174 / 174 通过，包含已有未提交修复，不能作为本次单独提交的全量结果 |
| 从 Git 暂存区独立导出的本次提交 | 全量 169 项，154 通过，15 失败 |
| 原始 HEAD 独立工作树对照 | 同样的 15 项引擎测试失败；本次提交未新增失败项 |

独立提交的失败集中于已有负向证据声明、查询伪事件语义、证据包和自动触发约束测试。本次没有把工作区中针对这些问题的既有未提交修改一并提交。

## 新增回归覆盖

- 三类工作流的项目发现、筛选和只读加载。
- 活动与归档冲突、损坏 JSON 和重复 ID。
- 当前检查点审核、旧文档版本拒绝、未决选项选择、修改或拒绝理由。
- 历史归档、旧版状态和非当前检查点不可审核。
- 路径越界保护，复杂图保留原文，不编造关系。
- 六里程碑展示，不把计划当成已验证结果。
- 状态事务发布不可变投影，重复保存不创建无意义版本。
- 历史版本内容隔离，文档与投影冲突时禁止审核。

## 验证边界

原生渲染测试使用真实 OpenTUI 渲染器，但 SDK 宿主对象是测试替身，不等同于 Mobile Coder 的真实全生命周期测试。

另尝试在隔离项目启动本机 OpenCode 1.18.18；该次 PTY 会话停留在空白终端，未取得可判定的控制面板画面，已停止测试进程。因此不将该尝试记为宿主端到端通过，也未更改用户全局插件配置。

本次没有进行模型 Token 或 DeepEval 效果评测。浏览路径没有调用模型 API；不应由此推导整个 DDD 工作流零 Token。
